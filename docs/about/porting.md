# Provenance / Porting

Per-repo, per-branch, per-commit inventory of where every module in this
package came from — what was ported, what was deliberately left behind,
and what remains as a porting candidate.

--8<-- "PORTING.md"
